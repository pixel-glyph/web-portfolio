export interface NavLinkType {
  name: string;
  route: string;
}

export interface ProjectPreviewType {
  title: string;
  desc: string;
  imgSrc: string;
  imgAlt: string;
  url: string;
  hash: string;
}